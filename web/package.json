{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-side port of the host pipeline (config + toolpath) for the ATtiny3224 x RP2350 RS485 CNC motion controller.",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "vite demo --open"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@types/node": "^22.0.0",
    "@vitest/ui": "^2.1.0",
    "@xmldom/xmldom": "^0.9.0",
    "eslint": "^9.12.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.15.0",
    "vite": "^8.1.3",
    "vitest": "^2.1.0"
  }
}
