/**
 * main.cpp — doctest's implementation + entry point for the motion suite.
 *
 * Exactly ONE translation unit in this suite may define
 * DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN; every other file here just includes
 * <doctest.h>. Keeping it in a file of its own means adding a test_*.cpp never
 * has to think about it.
 */

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest.h>
